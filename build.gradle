ext {
    versionMajor = 2
    versionMinor = 4
    versionPatch = 0
    versionBuild = 0
}

group = 'com.sefford'
version = "${versionMajor}.${versionMinor}.${versionPatch}"

description = 'brender'

task wrapper(type: Wrapper) {
    gradleVersion = '3.5'
}



